# Command for build, deploy and delete

```
# Build TS script
npm run build

# Before first deployment, we need to install boostrap
cdk bootstrap

# Deploy stack
cdk deploy

# Delete stack
cdk destroy 
```
